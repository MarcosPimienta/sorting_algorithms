#include <stdio.h>
#include <stdlib.h>
#include "sort.h"
/**
 * main - Entry point
 *
 * Return: Always 0
 */
void insertion_sort_list(listint_t **list)
{
        listint_t *temp, *current;
        current = *list;

        while(current->next != NULL)
        {
        }
}
